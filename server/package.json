{
  "name": "pablocorp-ig-verify",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "rm -rf .cache/puppeteer && node -e \"(async()=>{await require('@puppeteer/browsers').install({cacheDir:require('path').join(process.cwd(),'.cache','puppeteer'),browser:'chrome',buildId:'131.0.6778.204'});})()\"",
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "puppeteer": "^23.0.0",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
